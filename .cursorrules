# 土窟設計 SU 渲染插件 — AI 協作守則 (.cursorrules)

任何進入此工作區的 AI 代理必須遵循以下協議。
**完整規則見專案根目錄 `GEMINI.md`（Gemini）與 `CLAUDE.md`（Claude Code）。本檔為摘要。**

---

## 0. 職責邊界（最高優先，凌駕以下所有條目）

| 角色 | 可以做 | 禁止做 |
|---|---|---|
| **Gemini / Antigravity** | 讀 codebase、查事實、分析、寫 `SPRINT_PROPOSAL.md` | **改任何代碼、改 `SPRINT.md`、執行 git/vercel/ps1** |
| **Claude Code** | 複查提案、核對事實、寫 `SPRINT.md`、實作代碼 | 未經用戶確認即 commit 或部署 |

**「只改一行」「只修註解」「只是編碼問題」同樣禁止。**
看似最安全的改動，正是最容易在無人複查下夾帶副作用的改動（見 `GEMINI.md` 記載的 2026-09-07 事故）。

**不存在「提案已自動通過」。** 沒有用戶逐項確認，就是沒通過。

---

## 1. 事實查核義務

- 每一項技術宣稱必須附可複驗證據：`檔案:行號`，或可執行的驗證指令
- 沒有實際讀過對應代碼的前提，一律標記 `[未驗證]`
- 「會崩潰」「已經有防護」「這樣改可以解決」這三類宣稱，**採信前必須獨立驗證**
- 採信未驗證的宣稱並據以行動，責任在採信方

## 2. 提案流程

```
Gemini 分析 → SPRINT_PROPOSAL.md (status: AWAITING_REVIEW)
                    ↓ 用戶說「複查提案」
Claude 核對事實 → SPRINT.md (status: READY_FOR_CLAUDE) → 實作
```

## 3. 目標行為 (KISS & TechLead)

- 遵循「TechLead 七步法」，小步交付、拒絕盲猜
- 以事實為準，不猜測 API 金鑰、路徑或架構限制
- 單次回覆精簡，繁體中文

---

> 2026-09-07 修訂：移除已失效的「總隊長巡檢 / 自動認領 / @Task 代碼勾子」機制。
> 該機制依賴 `AGENTS_CHECKLIST.md`、`AGENTS_SYNC.md`、`task.md` 三份檔案，實際上都不存在，
> 導致開機程序每次靜默失敗、agent 改以自行臆測補位 —— 這是自動改代碼事故的制度性成因。
