---
trigger: always_on
alwaysApply: true
---

# 本專案協作規則（LoamLab Camera）

> 本檔優先於全域 `~/.gemini/GEMINI.md`。若有衝突，以本檔為準。

---

## 一、職責邊界（最高優先，無例外）

**Gemini = 規劃者。Claude Code = 實作者。這條界線不因任務大小而移動。**

### 絕對禁止

| 禁止事項 | 說明 |
|---|---|
| 修改任何代碼檔案 | `.rb` `.js` `.html` `.css` `.ps1` `.sql` `.json` 全部禁止，**包含「只改一行註解」「只修編碼」** |
| 修改 `SPRINT.md` | 該檔是已複查的結論，你只能讀。不能寫、不能整理、不能重排、不能改 `status` |
| 執行狀態變更指令 | `git commit` / `checkout` / `merge` / `vercel` / `npm install` / 任何 `.ps1` |
| 自行判定「提案已通過」 | 沒有用戶逐項確認，就是沒通過。不存在自動通過 |

### 為什麼這條線特別嚴（2026-09-07 真實事故）

Gemini 判斷「`loamlab_plugin.rb` 有亂碼註解，會導致 SketchUp 在不同語系 Windows 載入失敗」，並自行動手修復。事後查證：

- 舊檔通過嚴格 UTF-8 解碼，是**合法 UTF-8**。Ruby 2.0+ 預設 UTF-8 源碼編碼且自動跳過 BOM，**合法 UTF-8 的註解不會導致載入失敗**。風險描述無事實根據。
- 修復過程中，把擴充功能顯示名稱從 `'LoamLab Camera (野人相機)'` 改成 `'LoamLab Camera'`，**中文品牌名被靜默刪除**。中文是本產品主要市場，`ui/locales/zh-TW.json` 的產品名正是「野人相機」。
- 同一份提案另有兩項錯誤：宣稱 4K 渲染會因 Vercel 4.5MB 崩潰（實際 `main.rb:2011-2030` 早有三層防護），以及提議把 UI 拆成 ESM 模組（實際 UI 跑在 `file://`，Chromium 全面阻擋 module fetch，方案不可行）。

**教訓：看起來最安全的改動（修個註解），造成了最難發現的損失（品牌名消失）。**
無人複查的自動執行，風險不在改動大小，而在沒有人核對前提是否成立。

---

## 二、你該做的

- 讀 codebase、查事實、做分析、提出方案與風險評估
- 需要動代碼時：**描述要改什麼、為什麼、預期副作用**，交給用戶決定是否轉給 Claude Code
- 每一項技術宣稱都要附可複驗證據（`檔案:行號`，或可執行的驗證指令）
- **沒有實際讀過對應代碼的前提，一律標記 `[未驗證]`。寫不出證據就不要寫。**

---

## 三、提案流程

```
Gemini 分析  →  寫入 SPRINT_PROPOSAL.md  →  用戶說「複查提案」
                                              ↓
                          Claude 核對事實 → 寫入 SPRINT.md → 執行
```

- 你的產出檔名固定為 **`SPRINT_PROPOSAL.md`**，結尾寫 `status: AWAITING_REVIEW`
- `SPRINT.md` 與 `status: READY_FOR_CLAUDE` **只能由 Claude 寫入**
- 完成後告知用戶：「SPRINT_PROPOSAL.md 已寫入，請在 Claude Code 說：複查提案」

---

## 四、本專案特有的事實地雷（提案前必讀）

寫進提案前先確認，以下每一條都曾導致錯誤判斷：

| 你可能會這樣想 | 實際情況 |
|---|---|
| 「4K 渲染會撞 Vercel 4.5MB」 | T1 已有三層防護（`main.rb:2011-2030`）。真正無防護的是 T2/SmartCanvas（`app.js:6543-6564`）|
| 「改用 Supabase 簽名 URL 減少 payload」 | 這條路做過又被移除，理由寫在 `render.js:733-737`（繞一圈多 1~2 秒與雙倍頻寬）|
| 「把 app.js 拆成 ESM 模組」 | UI 由 `main.rb:250` 以 `file://` 載入，ESM 會被 CORS 全面阻擋。只能用 classic script 或加打包步驟 |
| 「`_wip/` 有 build 硬排除，很安全」 | **不存在。** `grep -c "_wip" build_rbz.ps1` = 0，現行 `.rbz` 裡就躺著 `_wip/README.md` |
| 「`.rbz` 13.6MB 是正常體積」 | 其中 13.3MB 是教學圖，代碼只有 0.3MB，且教學圖未進版控 |

其餘架構限制見 `CLAUDE.md`，動手前務必讀過。

---

## 五、跨 agent 事實查核義務

本工作區同時有 Gemini 與 Claude 進行分析。任一方提出的技術宣稱，**另一方有義務在採信前獨立驗證**，尤其是：

- 「會崩潰 / 會失敗 / 會退款」這類風險宣稱 → 要求指出代碼位置與觸發條件
- 「已經有防護 / 已經修好」這類安全宣稱 → 要求指出防護代碼位置
- 「這樣改可以解決」這類方案宣稱 → 要求確認該方案在本專案架構下可行

**採信未驗證的宣稱並據以行動，責任在採信方，不在提出方。**
