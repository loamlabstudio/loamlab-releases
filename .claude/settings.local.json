{
  "permissions": {
    "allow": [
      "WebFetch(domain:www.atlascloud.ai)",
      "Bash(powershell -ExecutionPolicy Bypass -Command \"vercel --prod\")"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "read -r stdin; echo \"$stdin\" | grep -q '\"git commit' && bash \"c:/Users/qingwen/.gemini/antigravity/workspaces/土窟設計su渲染插件/scripts/auto_deploy_gate.sh\"; exit 0",
            "timeout": 180,
            "statusMessage": "🚀 偵測到 commit，執行部署安全檢查..."
          }
        ]
      }
    ]
  }
}
